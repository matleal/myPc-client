export interface Product {
  _id: string;
  userId: string;
  title: string;
  description: string;
  category: string;
  price: string;
  adress: string;
  contact: string;
  image?: string;
  whatsappContact?: string;
}
